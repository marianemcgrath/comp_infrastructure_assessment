# Adapted from ChatGPT:
# https://chatgpt.com/s/t_6943472eae7c8191b9bdd9a841773044

name: Run faang script and commit changes

on:
  schedule:
    # https://crontab.guru/#0_5_*_*_6
    - cron: '0 5 * * 6'  # Runs every Saturday at 5:00 AM UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' # Use the latest Python 3 version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run faang.py
        run: python faang.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Automated update from faang.py" || echo "No changes to commit"
          git push origin HEAD:main